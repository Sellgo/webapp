version: 2.1
orbs:
  aws-s3: circleci/aws-s3@1.0.0
jobs:
  build:
    docker:
      - image: circleci/node:7.10
    steps:
      - checkout
      - run: 
          name: install-npm
          command: npm install
      - run: 
          name: build
          command: npm run build
      - aws-s3/sync:
          filters:
            branches:
              only: master
          from: bucket
          to: 's3://${AWS_S3_BUCKET_NAME}'
          overwrite: true