import React from 'react';
// import { connect } from 'react-redux';

// /* Styles */
// import styles from './index.module.scss';

// interface Props {}

const CataloguePlanning = () => {
  return (
    <main>
      <h1> CATALOGUE PLANNING </h1>
    </main>
  );
};

// const mapStateToProps = (state: any) => {
//   return {};
// };

// export default connect(mapStateToProps)(CataloguePlanning);
export default CataloguePlanning;
