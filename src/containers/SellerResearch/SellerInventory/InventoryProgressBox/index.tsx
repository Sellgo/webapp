import React from 'react';
import { truncateString } from '../../../../utils/format';

/* Styling */
import styles from './index.module.scss';

const InventoryProgressBox = () => {
  return (
    <div className={styles.inventoryProgressBox}>
      <div className={styles.progressDetails}>
        {/* Type of progress */}
        <span className={styles.progressType}>Check Sellers</span>

        {/* Additional Product Details */}
        <span className={styles.productDetails}>
          {truncateString('Big thermos water bottle Insulated with steel… (50 chars)', 50)}
        </span>

        {/* Seller/Product ASIN/ID */}
        <span className={styles.infoId}>AU12349G1</span>

        {/* Progress Icon */}
        <span className={styles.progressIcon}>✅</span>

        {/* Progress Status */}
        <span className={styles.progressStatus}>Completed</span>
      </div>

      <div className={styles.progressDetails}>
        {/* Type of progress */}
        <span className={styles.progressType}>Check Sellers</span>

        {/* Additional Product Details */}
        <span className={styles.productDetails}>
          {truncateString('Big thermos water bottle Insulated with steel… (50 chars)', 50)}
        </span>

        {/* Seller/Product ASIN/ID */}
        <span className={styles.infoId}>AU12349G1</span>

        {/* Progress Icon */}
        <span className={styles.progressIcon}>✅</span>

        {/* Progress Status */}
        <span className={styles.progressStatus}>Completed</span>
      </div>

      <div className={styles.progressDetails}>
        {/* Type of progress */}
        <span className={styles.progressType}>Check Sellers</span>

        {/* Additional Product Details */}
        <span className={styles.productDetails}>
          {truncateString('Big thermos water bottle Insulated with steel… (50 chars)', 50)}
        </span>

        {/* Seller/Product ASIN/ID */}
        <span className={styles.infoId}>AU12349G1</span>

        {/* Progress Icon */}
        <span className={styles.progressIcon}>✅</span>

        {/* Progress Status */}
        <span className={styles.progressStatus}>Completed</span>
      </div>

      <div className={styles.progressDetails}>
        {/* Type of progress */}
        <span className={styles.progressType}>Check Sellers</span>

        {/* Additional Product Details */}
        <span className={styles.productDetails}>
          {truncateString('Big thermos water bottle Insulated with steel… (50 chars)', 50)}
        </span>

        {/* Seller/Product ASIN/ID */}
        <span className={styles.infoId}>AU12349G1</span>

        {/* Progress Icon */}
        <span className={styles.progressIcon}>✅</span>

        {/* Progress Status */}
        <span className={styles.progressStatus}>Completed</span>
      </div>

      <div className={styles.progressDetails}>
        {/* Type of progress */}
        <span className={styles.progressType}>Check Sellers</span>

        {/* Additional Product Details */}
        <span className={styles.productDetails}>
          {truncateString('Big thermos water bottle Insulated with steel… (50 chars)', 50)}
        </span>

        {/* Seller/Product ASIN/ID */}
        <span className={styles.infoId}>AU12349G1</span>

        {/* Progress Icon */}
        <span className={styles.progressIcon}>✅</span>

        {/* Progress Status */}
        <span className={styles.progressStatus}>Completed</span>
      </div>

      <div className={styles.progressDetails}>
        {/* Type of progress */}
        <span className={styles.progressType}>Check Sellers</span>

        {/* Additional Product Details */}
        <span className={styles.productDetails}>
          {truncateString('Big thermos water bottle Insulated with steel… (50 chars)', 50)}
        </span>

        {/* Seller/Product ASIN/ID */}
        <span className={styles.infoId}>AU12349G1</span>

        {/* Progress Icon */}
        <span className={styles.progressIcon}>✅</span>

        {/* Progress Status */}
        <span className={styles.progressStatus}>Completed</span>
      </div>

      <div className={styles.progressDetails}>
        {/* Type of progress */}
        <span className={styles.progressType}>Check Sellers</span>

        {/* Additional Product Details */}
        <span className={styles.productDetails}>
          {truncateString('Big thermos water bottle Insulated with steel… (50 chars)', 50)}
        </span>

        {/* Seller/Product ASIN/ID */}
        <span className={styles.infoId}>AU12349G1</span>

        {/* Progress Icon */}
        <span className={styles.progressIcon}>✅</span>

        {/* Progress Status */}
        <span className={styles.progressStatus}>Completed</span>
      </div>

      <div className={styles.progressDetails}>
        {/* Type of progress */}
        <span className={styles.progressType}>Check Sellers</span>

        {/* Additional Product Details */}
        <span className={styles.productDetails}>
          {truncateString('Big thermos water bottle Insulated with steel… (50 chars)', 50)}
        </span>

        {/* Seller/Product ASIN/ID */}
        <span className={styles.infoId}>AU12349G1</span>

        {/* Progress Icon */}
        <span className={styles.progressIcon}>✅</span>

        {/* Progress Status */}
        <span className={styles.progressStatus}>Completed</span>
      </div>

      <div className={styles.progressDetails}>
        {/* Type of progress */}
        <span className={styles.progressType}>Check Sellers</span>

        {/* Additional Product Details */}
        <span className={styles.productDetails}>
          {truncateString('Big thermos water bottle Insulated with steel… (50 chars)', 50)}
        </span>

        {/* Seller/Product ASIN/ID */}
        <span className={styles.infoId}>AU12349G1</span>

        {/* Progress Icon */}
        <span className={styles.progressIcon}>✅</span>

        {/* Progress Status */}
        <span className={styles.progressStatus}>Completed</span>
      </div>

      <div className={styles.progressDetails}>
        {/* Type of progress */}
        <span className={styles.progressType}>Check Sellers</span>

        {/* Additional Product Details */}
        <span className={styles.productDetails}>
          {truncateString('Big thermos water bottle Insulated with steel… (50 chars)', 50)}
        </span>

        {/* Seller/Product ASIN/ID */}
        <span className={styles.infoId}>AU12349G1</span>

        {/* Progress Icon */}
        <span className={styles.progressIcon}>✅</span>

        {/* Progress Status */}
        <span className={styles.progressStatus}>Completed</span>
      </div>
    </div>
  );
};

export default InventoryProgressBox;
